/**
 * @swagger
 * components:
 *   schemas:
 *     Book:
 *       type: object
 *       properties:
 *         _id:
 *           type: string
 *         name:
 *           type: string
 *         author:
 *           type: string
 *         description:
 *           type: string
 *         cover_img_url:
 *           type: string
 *         publication_date:
 *           type: string
 *           format: date-time
 *         category:
 *           type: string
 *         type:
 *           type: string
 *         buy_price:
 *           type: number
 *         borrow_price_per_day:
 *           type: number
 *         pdf_path:
 *           type: string
 */
